{{yield (hash
    paginator = (component "nucleus-pagination/paginator"
    currentPage = currentPage
    onPageSelect = (action 'getPage')
    pages = pages
    totalPages = totalPages
  )
  pageItems = pageItems)
}}