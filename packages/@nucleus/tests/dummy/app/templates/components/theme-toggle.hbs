<button class="mode-toggle" onclick={{action "onToggle"}}>
  {{#if dark}}
   {{nucleus-icon name="light" customClass="toggle-icon" size="large"}}
  {{else}}
   {{nucleus-icon name="dark" size="large"}}
  {{/if}}
</button>