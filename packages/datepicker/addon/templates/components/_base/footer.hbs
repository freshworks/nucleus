<div>
  {{nucleus-button
    label=(t "buttons.cancel" locale=locale)
    ariaLabel=(t "buttons.cancel" locale=locale)
    variant="secondary" 
    onClick=(action onCancelClick)
  }}{{nucleus-button
    label=(t "buttons.update" locale=locale)
    ariaLabel=(t "buttons.update" locale=locale)
    variant="primary" 
    onClick=(action OnUpdateClick)
  }}
</div>
