{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#if hasCloseButton}}
    {{nucleus-button
      variant="secondary"
      label=closeTitle
      onClick=(action onClose)}}
  {{/if}}
  {{#if hasSubmitButton}}
    {{nucleus-button
      disabled=submitDisabled
      variant=type
      label=submitTitle
      onClick=(action onSubmit)}}
  {{/if}}
{{/if}}