{{#if isOpen}}
  {{#ember-wormhole to="nucleus-modal-wormhole" renderInPlace=renderInPlace}}
    {{#nucleus-modal/dialog
      id=modalId
      onClose=(action "close")
      onSubmit=(action "submit")
      size=size
      keyboard=isDismissible
      backdrop=backdrop
      data-test-id=data-test-id
    }}
      {{nucleus-modal/header
        title=title
        icon=icon
        description=description
        closeButton=isDismissible
        closeButtonType="text"
        onClose=(action "close")}}
      {{#nucleus-modal/body}}{{yield}}{{/nucleus-modal/body}}
      {{nucleus-modal/footer
        type=type
        isSticky=false
        closeTitle=closeTitle
        submitTitle=submitTitle
        submitDisabled=submitDisabled
        onClose=(action "close")
        onSubmit=(action "submit")}}
    {{/nucleus-modal/dialog}}
  {{/ember-wormhole}}
{{/if}}
